<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    >

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".ui.activity.FormularioProdutosActivity">

        <EditText
            android:id="@+id/activity_formulario_produtos_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_margin="16dp"
            android:hint="Title"
            android:inputType="text"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <EditText
            android:id="@+id/activity_formulario_produtos_descricao"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:hint="Descrição"
            android:inputType="text"
            app:layout_constraintEnd_toEndOf="@id/activity_formulario_produtos_title"
            app:layout_constraintStart_toStartOf="@id/activity_formulario_produtos_title"
            app:layout_constraintTop_toBottomOf="@id/activity_formulario_produtos_title" />

        <EditText
            android:id="@+id/activity_formulario_produtos_preco"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:hint="Preço"
            android:inputType="numberDecimal"
            app:layout_constraintEnd_toEndOf="@id/activity_formulario_produtos_title"
            app:layout_constraintStart_toStartOf="@id/activity_formulario_produtos_title"
            app:layout_constraintTop_toBottomOf="@id/activity_formulario_produtos_descricao" />

        <Button
            android:id="@+id/activity_formulario_produtos_buttonSave"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_margin="16dp"
            android:text="Salvar"
            app:layout_constraintVertical_bias="1"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/activity_formulario_produtos_preco" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</ScrollView>
